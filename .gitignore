# dependencies
/node_modules

# next.js
/.next
/out

# production
/build

# local SQLite database — packing.db IS tracked (per request); only the
# transient WAL/SHM sidecar files stay out of git.
/data/packing.db-shm
/data/packing.db-wal
*.db-shm
*.db-wal

# misc
.DS_Store
*.pem

# env / secrets (Turso credentials, etc.)
.env
.env*.local

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# Excel lock file
~$*.xlsx
.vercel
